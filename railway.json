{
  "path": "server/python",
  "language": "python",
  "nixpacks": {
    "builder": "heroku/buildpacks:20"
  },
  "build": {
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python embedding_service.py"
  }
}
